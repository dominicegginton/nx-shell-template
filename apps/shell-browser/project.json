{
  "name": "shell-browser",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/shell-browser/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/packages/shell-browser"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "shell-browser:build",
        "proxyConfig": "apps/shell-browser/proxy.conf.json"
      },
      "configurations": {
        "development": {
          "buildTarget": "shell-browser:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "shell-browser:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "shell-browser:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "shell-browser:build:development"
        },
        "production": {
          "buildTarget": "shell-browser:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../../coverage/apps/shell-browser"
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/shell-browser/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
